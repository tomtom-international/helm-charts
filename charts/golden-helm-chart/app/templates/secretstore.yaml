{{- range $k, $v := .Values.secretStores -}}
{{- if not (eq $v nil) }}
{{ tpl (include "common.secretstore" (mergeOverwrite (deepCopy $.Values.common.secretStore) $v (dict "name" $k "common" $.Values.common "Values" $.Values))) (merge (dict "Template" (dict "BasePath" "<inline>" "Name" "<noname>")) $) }}
{{- end -}}
{{- end -}}
