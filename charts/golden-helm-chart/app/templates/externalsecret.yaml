{{- range $k, $v := .Values.externalSecrets -}}
{{- if not (eq $v nil) }}
{{ tpl (include "common.externalsecret" (mergeOverwrite (deepCopy $.Values.common.externalSecret) $v (dict "name" $k "common" $.Values.common "Values" $.Values))) (merge (dict "Template" (dict "BasePath" "<inline>" "Name" "<noname>")) $) }}
{{- end -}}
{{- end -}}
