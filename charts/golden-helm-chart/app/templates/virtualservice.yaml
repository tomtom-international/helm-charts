{{- range $k, $v := .Values.virtualservices -}}
{{- if not (eq $v nil) }}
{{ tpl (include "common.ingress" (mergeOverwrite (deepCopy $.Values.common.ingress) $v (dict "name" $k "common" $.Values.common "Values" $.Values))) (merge (dict "Template" (dict "BasePath" "<inline>" "Name" "<noname>")) $) }}
{{- end -}}
{{- end -}}
