{{- range $k, $v := .Values.virtualServices -}}
{{- if not (eq $v nil) }}
{{ tpl (include "common.virtualservice" (mergeOverwrite (deepCopy $.Values.common.virtualService) $v (dict "name" $k "common" $.Values.common "Values" $.Values))) (merge (dict "Template" (dict "BasePath" "<inline>" "Name" "<noname>")) $) }}
{{- end -}}
{{- end -}}
