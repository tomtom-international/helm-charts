{{- range $k, $v := .Values.applications -}}
{{- if not (eq $v nil) }}
{{ tpl (include "common.application" (mergeOverwrite (deepCopy $.Values.common.application) $v (dict "name" $k "common" $.Values.common "Values" $.Values))) (merge (dict "name" $k "Template" (dict "BasePath" "<inline>" "Name" "<noname>")) $) }}
{{- end -}}
{{- end -}}
