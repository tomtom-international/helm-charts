{{- range $k, $v := .Values.authorizationPolicies -}}
{{- if not (eq $v nil) }}
{{ tpl (include "common.authorizationpolicy" (mergeOverwrite (deepCopy $.Values.common.authorizationPolicy) $v (dict "name" $k "common" $.Values.common "Values" $.Values))) (merge (dict "Template" (dict "BasePath" "<inline>" "Name" "<noname>")) $) }}
{{- end -}}
{{- end -}}
