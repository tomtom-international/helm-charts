{{- range $k, $v := .Values.gateways -}}
{{- if not (eq $v nil) }}
{{ tpl (include "common.gateway" (mergeOverwrite (deepCopy $.Values.common.gateway) $v (dict "name" $k "common" $.Values.common "Values" $.Values))) (merge (dict "Template" (dict "BasePath" "<inline>" "Name" "<noname>")) $) }}
{{- end -}}
{{- end -}}
