{{- range $k, $v := .Values.pods -}}
{{- if not (eq $v nil) }}
{{ tpl (include "common.pod" (mergeOverwrite (deepCopy $.Values.common.pod) $v (dict "name" $k "common" $.Values.common "Values" $.Values))) (merge (dict "Template" (dict "BasePath" "<inline>" "Name" "<noname>")) $) }}
{{- end -}}
{{- end -}}
