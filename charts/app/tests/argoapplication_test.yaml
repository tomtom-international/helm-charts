suite: test argoapplication
templates:
  - manifests.yaml
tests:
  - it: should work
    values:
      - ./fixtures/argoapplication.yaml
    asserts:
      - isKind:
          of: Application
      - equal:
          path: spec
          value:
            sources:
              - path: a
              - path: b
            destination:
              server: https://kubernetes.default.svc
              namespace: ns
            syncPolicy:
              automated: {}
