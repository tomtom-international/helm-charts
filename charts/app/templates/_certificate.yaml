{{- define "common.certificate" -}}
{{- /* reducers: default to own name for spec.target.secretName */}}
{{- $defaults := dict "secretName" .metadata.name }}
{{- if .spec.target }}{{ $_ := merge .spec $defaults }}{{ end }}
{{- end -}}
